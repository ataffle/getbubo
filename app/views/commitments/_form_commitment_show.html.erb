<% if @commitment.invoice? %>
  <i class="fas fa-paperclip"></i>
<% else %>
  <%= simple_form_for(@commitment) do |f| %>
    <%= f.input :invoice %>
    <%= f.input :invoice_cache, as: :hidden %>
    <%= f.submit "Add this invoice", class: "btn btn-primary", method: :put %>
  <% end %>
<% end %>
